"use client";
import {useRef,useState} from "react";
import type {ProductData,ChampionshipKind} from "../product/frozen-product-data";
import type {HeadToHeadResult} from "../product/head-to-head";
import {parseHeadToHeadResult} from "../product/head-to-head-result";
import {StandingsPanel} from "./standings-panel";import {ScenarioDossier} from "./scenario-dossier";
async function reason(response:Response){try{const value:unknown=await response.json();if(value&&typeof value==="object"&&"reason" in value&&typeof value.reason==="string")return value.reason}catch{}return "The comparison could not be completed."}
export function ScenarioWorkbench({data}:{data:ProductData}){const[kind,setKind]=useState<ChampionshipKind>("driver"),[targetId,setTargetId]=useState(""),[rivalId,setRivalId]=useState(""),[result,setResult]=useState<HeadToHeadResult|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState<string|null>(null),pending=useRef(false);function clear(){setResult(null);setError(null)}function switchKind(next:ChampionshipKind){setKind(next);setTargetId("");setRivalId("");clear()}async function compare(){if(pending.current||!targetId||!rivalId)return;pending.current=true;setBusy(true);clear();try{const response=await fetch("/api/compare",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({kind,targetId,rivalId,dataVersion:data.dataVersion,ruleVersion:data.ruleVersion})});if(!response.ok)throw new Error(await reason(response));const value=parseHeadToHeadResult(await response.json());if(!value||value.targetId!==targetId||value.rivalId!==rivalId||value.dataVersion!==data.dataVersion)throw new Error("The server returned a stale or invalid comparison.");setResult(value)}catch(value){setError(value instanceof Error?value.message:"The comparison could not be completed.")}finally{pending.current=false;setBusy(false)}}return <div className="workbench"><StandingsPanel kind={kind} rows={data.standings[kind]} onKind={switchKind}/><ScenarioDossier data={data} kind={kind} targetId={targetId} rivalId={rivalId} result={result} busy={busy} error={error} onTarget={id=>{setTargetId(id);setRivalId("");clear()}} onRival={id=>{setRivalId(id);clear()}} onCalculate={()=>void compare()}/></div>}
