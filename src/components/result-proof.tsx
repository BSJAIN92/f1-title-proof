"use client";
import type { ResultView } from "../product/calculate-scenario";
export function ResultProof({result}:{result:ResultView}){if(result.status==="ELIMINATED")return <section className="result-block eliminated-block"><p className="eyebrow">Calculation complete</p><h2>Eliminated</h2><p>{result.reason}</p><div className="stop">STOP</div></section>;
 if(result.status!=="COMPLETE")return null;return <section className="result-block" aria-labelledby="proof-title"><p className="eyebrow">Exact championship dossier</p><h2 id="proof-title">The proof for {result.contenderId}</h2><div className="proof-rail"><article><span className="rail-index">01</span><div><h3>Winning rule</h3><p>{result.rule}</p></div></article>{result.groups?.map((g,index)=><article key={g.id}><span className="rail-index">0{index+2}</span><div><h3>{g.id==="POINTS_AHEAD"?"Points ahead":"Countback win"}</h3><p>{g.description}</p><code>{g.id}</code></div></article>)}<p className="relationship"><strong>{result.groupRelationship?.behavior}</strong> · {result.groupRelationship?.statement}</p><div className="stop">STOP · COMPLETE WINNING SET</div></div>
 <div className="sample-rail"><header><p className="eyebrow">Detached event replay</p><h3>Representative paths and boundaries</h3></header>{result.samples?.map(sample=><article key={sample.title}><strong>{sample.title}</strong><p>{sample.summary}</p>{sample.events?<details><summary>Event-by-event replay</summary><ol>{sample.events.map(event=><li key={event}>{event}</li>)}</ol></details>:null}<small>{sample.label}</small></article>)}</div></section>}
