"use client";
import type { HeadToHeadResult } from "../product/head-to-head";
export function ResultProof({result}:{result:HeadToHeadResult}){return <section className="result-block" aria-label="Comparison result"><div className="comparison-rule"><span>Complete winning rule</span><strong>{result.rule}</strong><p>Current gap: {result.currentGap>=0?"+":""}{result.currentGap}. A final tie is not counted as a head-to-head win.</p></div><div className="sample-rail">{result.examples.map(example=><article key={example.title}><strong>{example.title}</strong><p>{result.targetId} finishes {example.finalMargin} point{example.finalMargin===1?"":"s"} ahead.</p><details><summary>Show upcoming race positions</summary><ol>{example.events.map(event=><li key={event}>{event}</li>)}</ol></details></article>)}</div></section>}
