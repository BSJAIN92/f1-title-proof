"use client";
import type {ChampionshipKind,StandingView} from "../product/frozen-product-data";

export function StandingsPanel({kind,rows,busy,onKind,onCompare}:{kind:ChampionshipKind;rows:readonly StandingView[];busy:boolean;onKind:(kind:ChampionshipKind)=>void;onCompare:(targetId:string,rivalId:string)=>void}){
 return <main className="standings standings--home" aria-label="Championship standings"><header className="standings__head"><p className="eyebrow">2026 · Frozen standings</p><h1>Title proof</h1><div className="switch" aria-label="Championship"><button aria-pressed={kind==="driver"} onClick={()=>onKind("driver")}>Drivers</button><button aria-pressed={kind==="constructor"} onClick={()=>onKind("constructor")}>Constructors</button></div></header><div className="standings-table"><div className="table-head" aria-hidden="true"><span>Rank</span><span>Contender</span><span>Pts</span><span>Gap</span><span>Compare</span></div><ol className="standings-list">{rows.map(row=><li key={row.id}><div className="standing-row"><span className="rank">{String(row.position).padStart(2,"0")}</span><span className="contender"><strong>{row.id}</strong><small className={row.eligible?"eligible":"eliminated"}>{row.eligible?"In contention":"Eliminated"}</small></span><span className="number">{row.points}</span><span className="number gap">{row.gap===0?"LEAD":row.gap}</span><label className="compare-cell"><span className="sr-only">Compare {row.id} with</span><select value="" disabled={busy} aria-label={`Compare ${row.id} with`} onChange={event=>{const rivalId=event.target.value;if(rivalId)onCompare(row.id,rivalId)}}><option value="">Select rival</option>{rows.filter(rival=>rival.id!==row.id).map(rival=><option key={rival.id} value={rival.id}>{rival.id}</option>)}</select></label></div></li>)}</ol></div></main>
}
