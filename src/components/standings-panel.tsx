"use client";
import type { ChampionshipKind, StandingView } from "../product/frozen-product-data";
export function StandingsPanel({kind,rows,selectedId,onKind,onSelect}:{kind:ChampionshipKind;rows:readonly StandingView[];selectedId:string|null;onKind:(kind:ChampionshipKind)=>void;onSelect:(id:string)=>void}){
 return <aside className="standings" aria-label="Championship standings"><header className="standings__head"><p className="eyebrow">2026 · Frozen standings</p><h1>Title proof</h1><div className="switch" aria-label="Championship"><button aria-pressed={kind==="driver"} onClick={()=>onKind("driver")}>Drivers</button><button aria-pressed={kind==="constructor"} onClick={()=>onKind("constructor")}>Constructors</button></div></header>
 <div className="table-head" aria-hidden="true"><span>Rank / contender</span><span>Pts</span><span>Gap</span></div><ol className="standings-list">{rows.map(row=><li key={row.id}><button className="standing-row" aria-current={selectedId===row.id?"true":undefined} onClick={()=>onSelect(row.id)}><span className="rank">{String(row.position).padStart(2,"0")}</span><span className="contender"><strong>{row.id}</strong><small className={row.eligible?"eligible":"eliminated"}>{row.eligible?"In contention":"Eliminated"}</small></span><span className="number">{row.points}</span><span className="number gap">{row.gap===0?"LEAD":row.gap}</span></button></li>)}</ol></aside>;
}
